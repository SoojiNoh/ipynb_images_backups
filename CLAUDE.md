# 공동 규칙

## 응답 형식

모든 응답은 아래 두 항목으로 나눠서 쓴다. 순서도 이대로.

### 저 대신 해주실 것
Claude 가 직접 처리했거나 처리할 일. 이미 한 것은 완료형으로, 지금 할 것은 명확하게.
빈 경우에도 항목을 생략하지 말고 "없음 — 이유" 를 적는다.

### 직접 하실 것
사용자가 직접 해야만 하는 일. 각 항목마다 **왜 자동화할 수 없는지** 한 줄로 붙인다.
(예: 본인 Apple ID 자격증명, 물리적 기기 조작, GUI 승인 등)

자동화할 수 있는데 사용자에게 시키는 것은 규칙 위반이다.
"직접 하실 것" 이 길어지면 먼저 줄일 방법을 찾는다.

## 명령을 줄 때

- 여러 명령으로 나누지 말고 하나로 합친다.
- 재실행해도 안전해야 한다. `rm -rf` 처럼 한 번만 안전한 명령은 주지 않는다.
- 실패했을 때 원인이 화면에 남아야 한다. 조용히 죽는 스크립트는 만들지 않는다.

## 실행 환경

Claude 는 클라우드 리눅스 컨테이너에서 돌고, 사용자의 Mac / iPhone 은 별도 기계다.
그쪽에서 명령을 실행할 수단이 없으므로, 대신 **사용자가 한 번만 실행하면 되는
스크립트를 저장소에 만들어 두는 방식**으로 처리한다.
